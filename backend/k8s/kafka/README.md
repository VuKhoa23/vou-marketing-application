# Run each deployment one by one, from 1 to 3 using 

## kubectl apply f="NAME_OF_DEPLOYMENT.yml"

# Check the Minikube dashboard to see if the current deployment is green before proceeding to the next deployment.
